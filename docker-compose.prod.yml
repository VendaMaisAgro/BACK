services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: vendamais_backend
    env_file:
      - .env
    ports:
      - "5000:5000"
    restart: unless-stopped

